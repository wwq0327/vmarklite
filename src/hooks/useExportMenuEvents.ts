/**
 * Export Menu Events — Stubbed for read-only preview
 *
 * Read-only preview does not use export menu events.
 */

import { useEffect } from "react";

export function useExportMenuEvents(): void {
  useEffect(() => {}, []);
}

/**
 * Lint Navigation — Stubbed for read-only preview
 *
 * Read-only preview does not use lint navigation.
 */

export function scrollToSelectedDiagnostic(): void {}

export function clearPendingLintScroll(): void {}

export function consumePendingLintScroll(): number | undefined {
  return undefined;
}

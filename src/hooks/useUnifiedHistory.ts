/**
 * Unified History — Stubbed for read-only preview
 *
 * Read-only preview does not use unified history.
 */

export function performUnifiedUndo(): boolean {
  return false;
}

export function performUnifiedRedo(): boolean {
  return false;
}

export function toggleSourceModeWithCheckpoint(): void {}

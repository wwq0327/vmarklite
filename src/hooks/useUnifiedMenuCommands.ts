/**
 * Unified Menu Commands — Stubbed for read-only preview
 *
 * Read-only preview does not use menu commands.
 */

import { useEffect } from "react";

export function useUnifiedMenuCommands(): void {
  useEffect(() => {}, []);
}

/**
 * View Shortcuts — Stubbed for read-only preview
 *
 * Read-only preview does not use view shortcuts.
 */

import { useEffect } from "react";

export function useViewShortcuts(): void {
  useEffect(() => {}, []);
}

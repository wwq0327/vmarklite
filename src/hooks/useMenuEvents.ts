/**
 * Menu Events — Stubbed for read-only preview
 *
 * Read-only preview does not use menu events.
 */

import { useEffect } from "react";

export function useMenuEvents(): void {
  useEffect(() => {}, []);
}

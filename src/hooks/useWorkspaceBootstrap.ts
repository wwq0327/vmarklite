/**
 * Workspace Bootstrap — Stubbed for read-only preview
 *
 * Read-only preview does not use workspace bootstrap.
 */

import { useEffect } from "react";

export function useWorkspaceBootstrap(): void {
  useEffect(() => {}, []);
}

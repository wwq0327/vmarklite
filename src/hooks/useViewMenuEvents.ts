/**
 * View Menu Events — Stubbed for read-only preview
 *
 * Read-only preview does not use view menu events.
 */

import { useEffect } from "react";

export function useViewMenuEvents(): void {
  useEffect(() => {}, []);
}

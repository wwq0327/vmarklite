/**
 * Pre-Mode-Switch Cleanup - Stubbed for read-only preview
 *
 * Read-only preview does not have source mode, so no cleanup needed.
 */

export function cleanupBeforeModeSwitch(): void {
  // No-op: read-only preview has no mode switching
}

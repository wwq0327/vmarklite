/**
 * Markmap Export — Stubbed for read-only preview
 */

export function markmapToHtml(): string {
  return "";
}

export function setupMarkmapExport(_wrapper: HTMLElement, _sourceContent: string): unknown {
  return null;
}

/**
 * Source Popup — Stubbed for read-only preview
 */

export function useSourcePopup(): unknown {
  return null;
}

export function getPopupHostForDom(_anchor: HTMLElement | null): HTMLElement | null {
  return null;
}

export function toHostCoordsForDom(_host: HTMLElement, _coords: { top: number; left: number }): { top: number; left: number } {
  return { top: 0, left: 0 };
}

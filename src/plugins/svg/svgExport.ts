/**
 * SVG Export — Stubbed for read-only preview
 */

export function svgToHtml(): string {
  return "";
}

export function setupSvgExport(_wrapper: HTMLElement, _sourceContent: string): unknown {
  return null;
}

/**
 * Mermaid Export — Stubbed for read-only preview
 */

export function mermaidToHtml(): string {
  return "";
}

export function setupMermaidExport(_wrapper: HTMLElement, _sourceContent: string): unknown {
  return null;
}

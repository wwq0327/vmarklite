/**
 * GitHub Actions Workflow Xyflow Snapshot Render — Stubbed for read-only preview
 */

export function renderXyflowSnapshot(_yaml: string): Promise<string | null> {
  return Promise.resolve(null);
}

/**
 * Terminal Gate — Stubbed for read-only preview
 *
 * Read-only preview does not use terminal.
 */

export function TerminalGate(_props: { children: React.ReactNode }) {
  return null;
}

export function requestToggleTerminal(): void {}
